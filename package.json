{
  "name": "tracking-languages",
  "version": "1.0.0",
  "private": true,
  "description": "Track YouTube watch time by language",
  "scripts": {
    "test": "playwright test --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
